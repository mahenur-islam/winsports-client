import React from "react";
import { Table } from "flowbite-react";
const FeaturedTable = ({blog}) => {
    const {_id, title, shortDescription, details, image, category} = blog;
  return (
    <div>
    
    </div>
  );
};

export default FeaturedTable;
